function runRegExp() {
    regexp = document.querySelector('#regexp-custom').value;
    regexpInput = document.querySelector('#regexp-input').value;
    document.querySelector('#regexp-result').textContent = "essa coisa: " + regexp + "\n com essa: " + regexpInput + "\n faz algo mas não sei o que é";
}
